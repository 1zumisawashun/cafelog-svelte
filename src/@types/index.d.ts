export type Field = {
  shopName: string;
  station: string;
  photoUrls: Array<string>;
  comment: string;
  address: string;
  tel: string;
  tags: Array<string>;
  starRating: number;
  businessHours: string;
  openOrClose: string;
  longitude: string; // 緯度
  latitude: string; // 経度
};
